# ============================================================
# Link in Bio — Environment Variables
# ============================================================
# Copy file นี้เป็น .env.local แล้วกรอกค่าของตัวเอง
# cp .env.example .env.local
# ============================================================

# ไม่ต้องการ env สำหรับ project นี้
# เป็น static site ทั้งหมด — แก้ที่ config.json เท่านั้น

# Optional: ถ้าต้องการ Analytics
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
